bool isCovered(int** ranges, int rangesSize, int* rangesColSize, int left, int right) {
    int sack[52]={0};
    for(int i=0;i<rangesSize;i++)
    {
        int start=ranges[i][0];
        int end=ranges[i][1];
        sack[start]++;
        sack[end+1]--;
    }
    for(int i=1;i<52;i++)
    {
        sack[i]=sack[i]+sack[i-1];
    }
    for(int i=left;i<=right;i++)
    if(sack[i]<=0)
    {return false;
    }
return true;
}
